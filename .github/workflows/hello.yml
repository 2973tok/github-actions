name: Merhaba Dünya

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  selamla:
    runs-on: ubuntu-latest
    steps:
      - name: Bir mesaj yazdır
        run: echo "Merhaba GitHub Actions!"

      - name: Tarihi göster
        run: date

      - name: Klasörü listele
        run: ls -la
